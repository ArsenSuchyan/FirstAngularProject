import { DateOfIssuePipe } from './date-of-issue.pipe';

describe('DateOfIssuePipe', () => {
  it('create an instance', () => {
    const pipe = new DateOfIssuePipe();
    expect(pipe).toBeTruthy();
  });
});
